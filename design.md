# Design

The detailed design is served from `docs/design.md`.

This root file is intentionally a pointer. The design docs map the service
boundary, backend adapters, persistence, telemetry, benchmark evidence, and
future Mac Studio cluster routing plan.
