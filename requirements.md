# Requirements

The detailed requirements are served from `docs/requirements.md`.

This root file is intentionally a pointer so clone-time agent context stays
small and the MkDocs site remains the main source for requirements detail.
